# Creating a separate node package to include in projects package.json  

* folder structure  
  * bin directory do you need an executable to create a valid package.
  * only require a package.json file to be a package.  
  
