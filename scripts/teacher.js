var isTeacher = function(name) {
  if (name === 'Dan' || name === 'Andrew') {
    console.log('true');
    return true;
  }
  else { 
    console.log('false');
    return false;
  }
}
//isTeacher('Dan');
// isTeacher('Andrew');
isTeacher('Dick');
